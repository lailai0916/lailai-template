---
paths:
  - 'src/**/*.{ts,tsx}'
  - 'src/**/*.css'
---

# <Area> rules

<!--
Rename this file to <name>.md and register it in the project's AGENTS.md.
Replace the example paths and prose with durable local behavior, then remove
this note. Leave broad repository standards at their canonical upstream URL.
-->

One or two sentences on what this rule governs and when it applies.

## <Section>

- State concrete, checkable conventions for this project's matching paths.
- Explain non-obvious constraints so the rule remains useful during refactoring.
